import styled from "styled-components"

const ContainerExit = styled.section`
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;

    h1{
        font-size: 3rem;
        color: var(--light-color);
        margin-bottom: 2%;
    }

    button{
        width: 30%;
        height: 7%;
        border-radius: 1rem;
        background-color: var(--secondary-color);
        border: none;
        font-size: 2rem;
        color: var(--light-color);
    }

`

export default ContainerExit